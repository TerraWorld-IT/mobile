<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Config (Android 7.0+).

    base-config: 전역 기본 — cleartext 차단 + system CA 만 신뢰.
    domain-config (cleartextTrafficPermitted="true"):
      - 10.0.2.2 (Android emulator host alias) — 로컬 dev 서버 접속용
      - 10.0.3.2 (Genymotion alias)
      - localhost / 127.0.0.1 — 일부 디바이스 디버그용

    debuggable=true 빌드 (debug APK) 일 때만 cleartext 허용. release APK 는
    `<application android:usesCleartextTraffic="false">` 와 결합하여 어떤 호스트에도
    HTTP 송신 차단.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
