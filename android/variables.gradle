ext {
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
    // @capacitor-community/admob 의 android/build.gradle 이
    // `project.hasProperty('playServicesAdsVersion') ? rootProject.ext.playServicesAdsVersion : '24.9.+'`
    // 로 본 변수를 우선 읽는다 (미선언 시 동적 '24.9.+' — 재현성 저해).
    // 현재 '24.9.+' 의 실제 해석 버전 24.9.0 으로 핀 (Google Maven 24.9.x 최신 = 24.9.0 확인).
    playServicesAdsVersion = '24.9.0'
}